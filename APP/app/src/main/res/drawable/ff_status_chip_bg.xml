<?xml version="1.0" encoding="utf-8"?>
<!-- Chip trạng thái cho OrderDetailFragment -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <!-- Adapter/Fragment sẽ đổi màu nền runtime -->
    <solid android:color="@android:color/transparent"/>
    <corners android:radius="999dp"/>
    <stroke android:width="1dp" android:color="@color/ff_border"/>
    <padding
        android:left="10dp"
        android:top="4dp"
        android:right="12dp"
        android:bottom="4dp"/>
</shape>
