<?xml version="1.0" encoding="utf-8"?>
<!-- Chip trạng thái dùng trong item_merchant_order, item_order_history và item_order_item -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <!-- Màu sẽ được Adapter setColor() ghi đè (cho trạng thái) hoặc giữ trong suốt (cho số lượng) -->
    <solid android:color="@android:color/transparent"/>
    <corners android:radius="999dp"/>
    <stroke android:width="1dp" android:color="@color/ff_border"/>
    <padding
        android:left="10dp"
        android:top="4dp"
        android:right="10dp"
        android:bottom="4dp"/>
</shape>