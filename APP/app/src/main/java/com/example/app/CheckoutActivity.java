package com.example.app; // Đã chuyển sang package con activities

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;

/**
 * CheckoutActivity: Hoạt động cho màn hình thanh toán.
 * Đã chuyển sang package 'activities' để dễ quản lý.
 */
public class CheckoutActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        // TODO: Thiết lập layout thực tế cho màn hình thanh toán
        // setContentView(R.layout.activity_checkout);
    }
}